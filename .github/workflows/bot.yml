name: Telegram Bot - Direct Publishing

on:
  schedule:
    - cron: '0 6 * * *'    # 09:00 МСК (06:00 UTC)
    - cron: '0 11 * * *'   # 14:00 МСК (11:00 UTC)
    - cron: '0 16 * * *'   # 19:00 МСК (16:00 UTC)
  workflow_dispatch:        # Ручной запуск

jobs:
  generate-and-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        
      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: 🤖 Generate and Publish Posts
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          ADMIN_CHAT_ID: ${{ secrets.ADMIN_CHAT_ID }}
          MANAGER_GITHUB_TOKEN: ${{ secrets.MANAGER_GITHUB_TOKEN }}
          REPO_NAME: ${{ secrets.REPO_NAME }}
        run: |
          echo "🚀 Запуск Telegram Bot с прямой публикацией..."
          echo "=" * 60
          
          # Проверяем наличие обязательных переменных
          if [ -z "$BOT_TOKEN" ]; then
            echo "❌ ОШИБКА: BOT_TOKEN не установлен!"
            exit 1
          fi
          
          if [ -z "$GEMINI_API_KEY" ]; then
            echo "❌ ОШИБКА: GEMINI_API_KEY не установлен!"
            exit 1
          fi
          
          if [ -z "$PEXELS_API_KEY" ]; then
            echo "⚠️ ВНИМАНИЕ: PEXELS_API_KEY не установлен! Будут использоваться дефолтные картинки"
          fi
          
          if [ -z "$MANAGER_GITHUB_TOKEN" ]; then
            echo "⚠️ ВНИМАНИЕ: MANAGER_GITHUB_TOKEN не установлен! Функции GitHub API недоступны"
          else
            echo "✅ MANAGER_GITHUB_TOKEN: Установлен (длина: ${#MANAGER_GITHUB_TOKEN} символов)"
          fi
          
          if [ -n "$REPO_NAME" ]; then
            echo "✅ REPO_NAME: $REPO_NAME"
          fi
          
          echo "✅ Все переменные установлены"
          echo "📤 Режим: Прямая публикация в каналы"
          echo "🎯 Целевые каналы: @da4a_hr (TG) и @tehdzenm (Дзен)"
          echo "🔑 GitHub API: ${MANAGER_GITHUB_TOKEN:+✅ Доступен}${MANAGER_GITHUB_TOKEN:-\u26a0 Недоступен}"
          echo "=" * 60
          
          # Запускаем бота
          echo "🔄 Запуск генерации и публикации..."
          python github_bot.py
          
          # Проверяем результат
          EXIT_CODE=$?
          if [ $EXIT_CODE -eq 0 ]; then
            echo "✅ Выполнение завершено успешно"
          else
            echo "❌ Выполнение завершено с ошибкой (код: $EXIT_CODE)"
            exit $EXIT_CODE
          fi
          
      - name: 📊 Display Result
        if: always()
        run: |
          echo "=" * 60
          echo "📋 ИТОГИ ЗАПУСКА:"
          echo "=" * 60
          
          echo "🎯 Автоматическая публикация завершена"
          echo ""
          echo "📤 Посты опубликованы в каналы:"
          echo "• @da4a_hr (Telegram) — живой динамичный стиль с эмодзи"
          echo "• @tehdzenm (Яндекс.Дзен) — аналитический стиль без эмодзи"
          echo ""
          echo "⏰ Расписание публикаций (МСК):"
          echo "• 09:00 — Утренний пост"
          echo "• 14:00 — Дневной пост"
          echo "• 19:00 — Вечерний пост"
          echo ""
          echo "✅ Бот работает в режиме прямой публикации"
          echo "🖼️ Картинки: Pexels API (бесплатный сервис)"
          echo "🤖 Генерация: Google Gemini AI"
          echo "🔐 GitHub API: ${MANAGER_GITHUB_TOKEN:+✅ Включен}${MANAGER_GITHUB_TOKEN:-\u26a0 Выключен}"
          echo ""
          echo "⏰ Следующий запуск по расписанию"
          echo "=" * 60
