# 🤖 Telegram Bot - Автоматическая публикация постов

Автоматический бот для генерации и публикации контента в Telegram и Яндекс.Дзен.

## 🚀 Возможности

- **Автоматическая генерация** постов через Gemini AI
- **Умный выбор тем** (HR, PR, ремонт и строительство)
- **19 форматов подачи** текста
- **Автопубликация** в 2 канала одновременно:
  - Telegram: @da4a_hr
  - Яндекс.Дзен: @tehdzenm
- **История публикаций** для избежания повторов
- **Уведомления** администратору о публикациях

## ⏰ Расписание публикаций (МСК)

- **09:00** - Утренний пост (400-600 символов для TG, 1000-1500 для Дзен)
- **14:00** - Дневной пост (700-900 символов для TG, 700-850 для Дзен)
- **19:00** - Вечерний пост (600-900 символов для TG, 800-900 для Дзен)

## ⚙️ Настройка

### 1. Получите необходимые ключи:
- **BOT_TOKEN** от @BotFather в Telegram
- **GEMINI_API_KEY** от [Google AI Studio](https://makersuite.google.com/app/apikey)
- **ADMIN_CHAT_ID** (опционально) для уведомлений от @userinfobot

### 2. Настройте секреты в GitHub:
1. Перейдите в ваш репозиторий
2. Settings → Secrets and variables → Actions
3. Добавьте следующие секреты:
   - `BOT_TOKEN`
   - `GEMINI_API_KEY`
   - `CHANNEL_ID` (опционально, по умолчанию @da4a_hr)
   - `ADMIN_CHAT_ID` (опционально, для уведомлений)

## 📁 Структура проекта

- `.github/workflows/bot.yml` - GitHub Actions workflow
- `github_bot.py` - основной код бота
- `requirements.txt` - зависимости Python
- `.gitignore` - игнорируемые файлы
- `.env.example` - пример переменных окружения

## 🛠️ Локальный запуск

```bash
# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env на основе .env.example
cp .env.example .env
# Отредактируйте .env файл

# Запустите бота
python github_bot.py --test    # Тестовый режим
python github_bot.py --once    # Однократный запуск
