import os
import requests
import random
import json
import hashlib
from datetime import datetime
from dotenv import load_dotenv

load_dotenv()

BOT_TOKEN = os.getenv("BOT_TOKEN")
MAIN_CHANNEL_ID = "@da4a_hr"
ZEN_CHANNEL_ID = "@tehdzenm"
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")

print("=" * 80)
print("🚀 УМНЫЙ БОТ: AI-ГЕНЕРАЦИЯ С ПРОВЕРКОЙ УНИКАЛЬНОСТИ")
print("=" * 80)

class SmartPostGenerator:
    def __init__(self):
        self.themes = ["HR и управление персоналом", "PR и коммуникации", "ремонт и строительство"]
        
        self.history_file = "post_history.json"
        self.post_history = self.load_post_history()
        
        # Надежные изображения
        self.theme_images = {
            "HR и управление персоналом": [
                "https://images.unsplash.com/photo-1552664730-d307ca884978?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
                "https://images.unsplash.com/photo-1542744173-8e7e53415bb0?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
            ],
            "PR и коммуникации": [
                "https://images.unsplash.com/photo-1432888622747-4eb9a8efeb07?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
                "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?ixlib=rb-4.0.3&w=1200&h=630&fit=crop", 
            ],
            "ремонт и строительство": [
                "https://images.unsplash.com/photo-1541888946425-d81bb19240f5?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
                "https://images.unsplash.com/photo-1504307651254-35680f356dfd?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
            ]
        }
        
        self.fallback_images = [
            "https://picsum.photos/1200/630",
            "https://placekitten.com/1200/630",
        ]

    def load_post_history(self):
        """Загружает историю постов с темами и датами"""
        try:
            if os.path.exists(self.history_file):
                with open(self.history_file, 'r', encoding='utf-8') as f:
                    data = json.load(f)
                    # Проверяем старый формат и конвертируем при необходимости
                    if isinstance(data, dict) and "posts" not in data:
                        # Старый формат - конвертируем в новый
                        new_data = {"posts": [], "themes": {}}
                        for channel_id, post_hashes in data.items():
                            new_data["themes"][channel_id] = []
                        return new_data
                    return data
            return {"posts": [], "themes": {}}
        except:
            return {"posts": [], "themes": {}}
    
    def save_post_history(self):
        try:
            with open(self.history_file, 'w', encoding='utf-8') as f:
                json.dump(self.post_history, f, ensure_ascii=False, indent=2)
        except Exception as e:
            print(f"⚠️ Ошибка сохранения истории: {e}")
    
    def get_last_themes(self, channel_id, count=3):
        """Возвращает последние N тем для канала"""
        channel_key = str(channel_id)
        if channel_key in self.post_history.get("themes", {}):
            themes = self.post_history["themes"][channel_key]
            return themes[-count:] if len(themes) >= count else themes
        return []
    
    def add_theme_to_history(self, channel_id, theme):
        """Добавляет тему в историю"""
        channel_key = str(channel_id)
        if "themes" not in self.post_history:
            self.post_history["themes"] = {}
        if channel_key not in self.post_history["themes"]:
            self.post_history["themes"][channel_key] = []
        
        # Добавляем тему и ограничиваем историю последними 10 темами
        self.post_history["themes"][channel_key].append(theme)
        if len(self.post_history["themes"][channel_key]) > 10:
            self.post_history["themes"][channel_key] = self.post_history["themes"][channel_key][-10:]
        
        self.save_post_history()
    
    def get_smart_theme(self, channel_id):
        """Выбирает тему с учетом истории"""
        last_themes = self.get_last_themes(channel_id, 2)  # Смотрим 2 последние темы
        
        available_themes = self.themes.copy()
        
        # Убираем последнюю тему если она была недавно
        if len(last_themes) > 0:
            if last_themes[-1] in available_themes:
                available_themes.remove(last_themes[-1])
                print(f"🎯 Исключили последнюю тему: {last_themes[-1]}")
        
        # Если осталась только одна тема - возвращаем её
        if not available_themes:
            available_themes = self.themes.copy()
        
        theme = random.choice(available_themes)
        print(f"🎯 Выбрана тема: {theme} (история: {last_themes})")
        return theme

    def get_reliable_image(self, theme):
        try:
            theme_image = random.choice(self.theme_images.get(theme, self.theme_images["HR и управление персоналом"]))
            print(f"🖼️ Используем изображение: {theme_image}")
            return theme_image
        except:
            fallback = random.choice(self.fallback_images)
            print(f"🖼️ Используем fallback: {fallback}")
            return fallback

    def generate_with_gemini(self, prompt):
        """Генерация контента через Gemini API"""
        try:
            print("🧠 Запрос к Gemini API...")
            url = f"https://generativelanguage.googleapis.com/v1/models/gemini-1.5-flash:generateContent?key={GEMINI_API_KEY}"
            
            response = requests.post(
                url,
                json={
                    "contents": [{
                        "parts": [{"text": prompt}]
                    }],
                    "generationConfig": {
                        "maxOutputTokens": 800,
                        "temperature": 0.9,
                    }
                },
                timeout=30
            )
            
            if response.status_code == 200:
                data = response.json()
                generated_text = data["candidates"][0]["content"]["parts"][0]["text"].strip()
                print("✅ Текст сгенерирован")
                return generated_text
            else:
                print(f"❌ Ошибка Gemini: {response.status_code}")
                return None
                
        except Exception as e:
            print(f"❌ Ошибка генерации: {e}")
            return None

    def generate_tg_post(self, theme):
        """Генерация поста для Telegram через AI"""
        # Получаем историю тем для контекста
        last_themes = self.get_last_themes(MAIN_CHANNEL_ID, 2)
        theme_context = f"Последние темы: {', '.join(last_themes)}" if last_themes else ""

        prompt = f"""
        Ты опытный маркетолог с 30-летним опытом. Напиши УНИКАЛЬНЫЙ пост для Telegram на тему "{theme}" за 2024-2025 год.

        {theme_context}
        Важно: этот пост должен отличаться от предыдущих публикаций!

        Требования:
        - Пиши как настоящий человек, а не как корпоративный бот
        - Будь профессионален, но естественен
        - Избегай клише и корпоративного жаргона
        - Пост должен содержать:
          1. Цепляющий заголовок
          2. 2-3 актуальных факта или тренда
          3. 3 практических совета что делать прямо сейчас
          4. Вопрос для вовлечения аудитории
        - Длина: 400-600 символов
        - Не повторяй идеи из предыдущих постов
        - Говори о реальных кейсах, а не теории

        Тема: {theme}
        """
        
        generated_text = self.generate_with_gemini(prompt)
        
        if generated_text:
            # Добавляем хештеги
            hashtags = self.add_tg_hashtags(theme)
            return f"{generated_text}\n\n{hashtags}"
        else:
            # Fallback если AI не сработал
            return self.generate_fallback_tg_post(theme)

    def generate_zen_post(self, theme):
        """Генерация поста для Дзена через AI"""
        # Получаем историю тем для контекста
        last_themes = self.get_last_themes(ZEN_CHANNEL_ID, 2)
        theme_context = f"Последние темы: {', '.join(last_themes)}" if last_themes else ""

        prompt = f"""
        Ты опытный маркетолог с 30-летним опытом. Напиши УНИКАЛЬНЫЙ аналитический пост для Яндекс.Дзен на тему "{theme}" за 2024-2025 год.

        {theme_context}
        Важно: этот пост должен отличаться от предыдущих публикаций!

        Требования:
        - Стиль: профессиональная аналитика, но человеческим языком
        - Структура с подзаголовками
        - Без корпоративного жаргона и клише
        - Конкретные примеры и цифры
        - Без эмодзи и хештегов
        - Пост должен содержать:
          1. Заголовок (SEO-оптимизированный)
          2. Введение с актуальными данными
          3. 3 ключевых направления с подзаголовками
          4. Конкретные рекомендации для каждого направления
          5. Заключение с выводами
        - Длина: 600-900 символов
        - Не повторяй идеи из предыдущих постов

        Тема: {theme}
        """
        
        generated_text = self.generate_with_gemini(prompt)
        
        if generated_text:
            return generated_text
        else:
            # Fallback если AI не сработал
            return self.generate_fallback_zen_post(theme)

    def generate_fallback_tg_post(self, theme):
        """Резервный пост для Telegram"""
        fallbacks = {
            "HR и управление персоналом": [
                """HR в 2024: нанимают не навыки, а mindset

78% компаний ищут кандидатов с эмоциональным интеллектом • AI обрабатывает 65% резюме на старте

Что работает прямо сейчас:

Смотрите на soft skills — технике научить проще, чем поменять мышление
Внедрите пробный день вместо многоэтапных собеседований
Давайте фидбек даже отказанным кандидатам — это builds employer brand

А что сработало в вашей практике?

#HR #управление #команда #карьера""",
                
                """Как удерживать команду в 2024

68% сотрудников ценят обучение выше зарплаты • Гибкий график стал стандартом ожидания

Что работает прямо сейчас:

Создайте карту развития для каждой позиции — люди должны видеть рост
Разрешите работать над личными проектами 10% времени — снижает выгорание
Проводите one-to-one встречи о целях, а не задачах

Как мотивируете свою команду?

#HR #управление #команда #развитие"""
            ],
            
            "PR и коммуникации": [
                """PR в 2024: что реально работает

LinkedIn стал главной B2B-площадкой — 85% компаний ведут там PR • Короткие видео дают +300% к вовлеченности

Что работает прямо сейчас:

Делайте личный бренд руководителей — люди доверяют людям, а не компаниям
Снимите 30-секундное видео вместо пресс-релиза — его досмотрят до конца
Дайте эксперту пообщаться в комментариях — это живая коммуникация

Какие тренды пробовали?

#PR #коммуникации #LinkedIn #маркетинг""",
                
                """Коммуникации, которые доходят до людей

45% PR-специалистов используют AI для материалов • Микро-инфлюенсерам доверяют в 3 раза больше

Что работает прямо сейчас:

Говорите на языке клиента — проверьте, поймет ли ваша бабушка
Показывайте закулисье проектов — прозрачность builds trust быстрее
Отвечайте на комментарии лично — превращайте аудиторию в сообщество

Что лучше всего сработало у вас?

#PR #коммуникации #бренд #сообщество"""
            ],
            
            "ремонт и строительство": [
                """Ремонт в 2024: тренды, которые останутся

72% клиентов выбирают натуральные материалы • Умный дом стал must-have для 60% новостроек

Что работает прямо сейчас:

Используйте локальные материалы — экологичнее и поддерживает производителей
Заложите умные системы на этапе черновой отделки — переделывать дороже
Сделайте универсальную базу — сменный декор дешевле нового ремонта

Что из этого уже пробовали?

#ремонт #стройка #дизайн #интерьер""",
                
                """Строительство без стресса в 2024

Модульные конструкции сокращают сроки на 40% • Дроны экономят 25% бюджета на измерениях

Что работает прямо сейчас:

Создайте цифровой двойник объекта — избежите 80% ошибок на стройке
Используйте BIM-моделирование — видите проблемы до начала работ
Ведите онлайн-дневник стройки — клиент в курсе, вы меньше отвлекаетесь

Как оптимизируете процессы?

#ремонт #стройка #технологии #оптимизация"""
            ]
        }
        return random.choice(fallbacks.get(theme, ["Актуальные тренды 2024-2025. #тренды"]))

    def generate_fallback_zen_post(self, theme):
        """Резервный пост для Дзена"""
        fallbacks = {
            "HR и управление персоналом": [
                """Как изменился HR в 2024: новые правила найма и удержания

78% компаний теперь ищут кандидатов с развитым эмоциональным интеллектом, а AI обрабатывает 65% резюме на первичном этапе. Современный HR стал более человечным и технологичным одновременно.

Ключевые направления:

Мягкие навыки вместо жестких требований
Техническим навыкам можно научить, а мышление меняется долго. Смотрите на adaptability и learning agility.

Прозрачность процесса найма
Внедрите пробный день вместо пятиэтапного собеседования — так понятнее и кандидату, и работодателю.

Постоянная обратная связь
Давайте фидбек даже тем, кого не взяли — это формирует лояльность к бренду работодателя на годы вперед.

Профессионалы в HR теперь должны совмещать понимание человеческой психологии с умением работать с современными инструментами анализа данных.""",
                
                """Современные подходы к управлению персоналом в 2024 году

68% сотрудников остаются в компаниях где есть возможности для развития, при этом гибкий график перестал быть преимуществом и стал стандартом ожидания.

Ключевые направления:

Развитие вместо денежных стимулов
Создавайте карты профессионального роста для каждой позиции. Люди должны видеть свой путь в компании.

Баланс работы и личных интересов
Разрешение работать над персональными проектами 10% рабочего времени значительно снижает уровень профессионального выгорания.

Открытая коммуникация с руководством
Регулярные встречи один-на-один должны фокусироваться на карьерных целях и психологическом состоянии, а не на операционных задачах.

Эффективное управление сегодня строится на создании среды где сотрудники могут реализовывать свой потенциал."""
            ],
            
            "PR и коммуникации": [
                """PR в 2024: коммуникации, которые действительно доходят до людей

LinkedIn стал главной B2B-площадкой — 85% компаний ведут там PR, а короткие видео дают +300% к вовлеченности compared с текстовым контентом.

Ключевые направления:

Личный бренд вместо корпоративного
Люди доверяют людям, а не компаниям. Выступление CEO в LinkedIn часто эффективнее корпоративного поста.

Видеоконтент вместо текстового
Короткие видео до 1 минуты собирают в 3 раза больше просмотров. Зрители хотят видеть лица и эмоции.

Диалог с аудиторией вместо монолога
Комментарии под постами стали важнее самого контента. Аудитория ждет ответов и обсуждения.

Успех в PR сегодня — это умение говорить с аудиторией на ее языке, а не транслировать корпоративные месседжи.""",
                
                """Трансформация PR-коммуникаций в цифровую эпоху

45% PR-специалистов уже используют искусственный интеллект для подготовки материалов, при этом аудитория в три раза чаще доверяет микро-инфлюенсерам чем знаменитостям.

Ключевые направления:

Простота и ясность коммуникации
Говорите на языке вашей целевой аудитории, избегая профессионального жаргона. Тестируйте — поймет ли ваш контент человек далекий от индустрии.

Прозрачность и аутентичность
Демонстрация закулисья проектов и процессов создает доверие быстрее чем идеально отполированная картинка.

Формирование сообщества
Персональные ответы на комментарии и вовлечение аудитории в обсуждение превращает пассивных подписчиков в активное сообщество.

Современные PR требуют сочетания экспертизы в коммуникациях с глубоким пониманием цифровых платформ и психологии аудитории."""
            ],
            
            "ремонт и строительство": [
                """Строительство и ремонт в 2024: тренды, которые меняют индустрию

72% клиентов выбирают натуральные материалы даже если дороже, а умный дом стал must-have для 60% новостроек. Индустрия становится более экологичной и технологичной.

Ключевые направления:

Натуральные и локальные материалы
Использование местных материалов не только экологичнее, но и поддерживает локальных производителей.

Технологии как основа, а не дополнение
Заложите умные системы на этапе черновой отделки — последующая установка обойдется значительно дороже.

Гибкость и адаптивность пространства
Создавайте универсальную базу — сменный декор стоит дешевле, чем полномасштабный новый ремонт.

Профессионалы в индустрии теперь должны совмещать понимание материалов и технологий с умением создавать адаптивные пространства под changing needs клиентов.""",
                
                """Инновации в строительстве: как технологии меняют подходы в 2024

Модульные и сборные конструкции позволяют сократить сроки строительства на 40% без потери качества, а использование дронов и 3D-сканирования экономит до 25% бюджета на измерительных работах.

Ключевые направления:

Цифровое проектирование и моделирование
Создание цифровых двойников объектов помогает выявить и устранить до 80% потенциальных ошибок еще до начала строительных работ.

Прозрачность для заказчика
Ведение онлайн-дневника стройки в реальном времени позволяет клиенту быть в курсе прогресса и снижает количество запросов на отчетность.

Оптимизация через технологии
BIM-моделирование позволяет видеть clashes и несоответствия в проекте до начала монтажных работ, а не в процессе строительства.

Современное строительство требует интеграции цифровых инструментов на всех этапах — от проектирования до сдачи объекта."""
            ]
        }
        return random.choice(fallbacks.get(theme, ["Актуальные тенденции 2024-2025 года."]))

    def add_tg_hashtags(self, theme):
        hashtags = {
            "HR и управление персоналом": "#HR #управление #команда #карьера #работа",
            "PR и коммуникации": "#PR #коммуникации #LinkedIn #маркетинг #бренд", 
            "ремонт и строительство": "#ремонт #стройка #дизайн #интерьер #квартира"
        }
        return hashtags.get(theme, "")
    
    def send_to_telegram(self, chat_id, text, image_url=None):
        """Отправляет пост в Telegram"""
        print(f"📤 Отправка в {chat_id}...")
        
        if image_url:
            url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendPhoto"
            payload = {
                "chat_id": chat_id,
                "photo": image_url,
                "caption": text,
                "parse_mode": "HTML"
            }
            
            try:
                response = requests.post(url, json=payload, timeout=30)
                if response.status_code == 200:
                    self.add_to_history(text, chat_id)
                    self.add_theme_to_history(chat_id, self.current_theme)
                    print(f"✅ Пост отправлен в {chat_id}")
                    return True
                else:
                    print(f"❌ Ошибка: {response.text}")
                    return self.send_text_to_telegram(chat_id, text)
            except Exception as e:
                print(f"❌ Ошибка: {e}")
                return self.send_text_to_telegram(chat_id, text)
        else:
            return self.send_text_to_telegram(chat_id, text)
    
    def send_text_to_telegram(self, chat_id, text):
        """Отправляет текстовый пост"""
        url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
        payload = {
            "chat_id": chat_id,
            "text": text,
            "parse_mode": "HTML"
        }
        
        try:
            response = requests.post(url, json=payload, timeout=30)
            if response.status_code == 200:
                self.add_to_history(text, chat_id)
                self.add_theme_to_history(chat_id, self.current_theme)
                print(f"✅ Текстовый пост отправлен в {chat_id}")
                return True
            else:
                print(f"❌ Ошибка: {response.text}")
                return False
        except Exception as e:
            print(f"❌ Ошибка: {e}")
            return False
    
    def generate_post_hash(self, text):
        return hashlib.md5(text.encode('utf-8')).hexdigest()
    
    def is_post_unique(self, post_text, channel_id):
        post_hash = self.generate_post_hash(post_text)
        channel_key = str(channel_id)
        
        if "posts" not in self.post_history:
            self.post_history["posts"] = {}
        if channel_key not in self.post_history["posts"]:
            self.post_history["posts"][channel_key] = []
        
        recent_posts = self.post_history["posts"][channel_key][-50:]
        return post_hash not in recent_posts
    
    def add_to_history(self, post_text, channel_id):
        post_hash = self.generate_post_hash(post_text)
        channel_key = str(channel_id)
        
        if "posts" not in self.post_history:
            self.post_history["posts"] = {}
        if channel_key not in self.post_history["posts"]:
            self.post_history["posts"][channel_key] = []
        
        self.post_history["posts"][channel_key].append(post_hash)
        if len(self.post_history["posts"][channel_key]) > 100:
            self.post_history["posts"][channel_key] = self.post_history["posts"][channel_key][-50:]
        
        self.save_post_history()
    
    def send_dual_posts(self):
        # Выбираем тему с учетом истории
        self.current_theme = self.get_smart_theme(MAIN_CHANNEL_ID)
        
        print(f"🎯 Умный выбор темы: {self.current_theme}")
        
        # Получаем изображение
        theme_image = self.get_reliable_image(self.current_theme)
        
        # Генерируем посты через AI
        print("🧠 Генерация УНИКАЛЬНЫХ постов через AI...")
        tg_post = self.generate_tg_post(self.current_theme)
        zen_post = self.generate_zen_post(self.current_theme)
        
        print(f"📝 ТГ-пост: {len(tg_post)} символов")
        print(f"📝 Дзен-пост: {len(zen_post)} символов")
        
        # Проверяем уникальность
        if not self.is_post_unique(tg_post, MAIN_CHANNEL_ID):
            print("⚠️ Пост для ТГ не уникален, генерируем заново...")
            return self.send_dual_posts()
            
        if not self.is_post_unique(zen_post, ZEN_CHANNEL_ID):
            print("⚠️ Пост для Дзена не уникален, генерируем заново...")  
            return self.send_dual_posts()
        
        # Отправляем
        print("📤 Отправка в @da4a_hr...")
        tg_success = self.send_to_telegram(MAIN_CHANNEL_ID, tg_post, theme_image)
        
        print("📤 Отправка в @tehdzenm...")
        zen_success = self.send_to_telegram(ZEN_CHANNEL_ID, zen_post, theme_image)
        
        if tg_success and zen_success:
            print("✅ УНИКАЛЬНЫЕ ПОСТЫ УСПЕШНО ОТПРАВЛЕНЫ!")
            return True
        else:
            print(f"⚠️ Есть ошибки: ТГ={tg_success}, Дзен={zen_success}")
            return tg_success or zen_success

def main():
    print("\n🚀 ЗАПУСК УМНОГО AI-ГЕНЕРАТОРА")
    print("🎯 Проверка истории перед генерацией")
    print("🎯 Исключение повторяющихся тем")
    print("🎯 Уникальный контент для каждого поста")
    print("=" * 80)
    
    bot = SmartPostGenerator()
    success = bot.send_dual_posts()
    
    if success:
        print("\n🎉 УСПЕХ! Уникальные AI-посты отправлены!")
    else:
        print("\n💥 ЕСТЬ ОШИБКИ ОТПРАВКИ!")
    
    print("=" * 80)

if __name__ == "__main__":
    main()
