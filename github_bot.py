import os
import requests
import datetime
import random
import sys
import json
import hashlib
from dotenv import load_dotenv

load_dotenv()

BOT_TOKEN = os.getenv("BOT_TOKEN")
MAIN_CHANNEL_ID = "@da4a_hr"
ZEN_CHANNEL_ID = "@tehdzenm"
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")

print("=" * 80)
print("🚀 УМНЫЙ БОТ: АКТУАЛЬНАЯ ИНФОРМАЦИЯ 2024-2025")
print("=" * 80)

class SmartPostGenerator:
    def __init__(self):
        self.themes = ["HR и управление персоналом", "PR и коммуникации", "ремонт и строительство"]
        self.author = "Маркетолог • SMM • PR • Копирайтер (40 лет опыта)"
        
        self.history_file = "post_history.json"
        self.post_history = self.load_post_history()
        
        self.theme_images = {
            "HR и управление персоналом": [
                "https://picsum.photos/1200/630?random=1&office",
                "https://picsum.photos/1200/630?random=2&team",
                "https://picsum.photos/1200/630?random=3&business"
            ],
            "PR и коммуникации": [
                "https://picsum.photos/1200/630?random=4&media",
                "https://picsum.photos/1200/630?random=5&communication", 
                "https://picsum.photos/1200/630?random=6&marketing"
            ],
            "ремонт и строительство": [
                "https://picsum.photos/1200/630?random=7&construction",
                "https://picsum.photos/1200/630?random=8&renovation",
                "https://picsum.photos/1200/630?random=9&design"
            ]
        }
        
        self.knowledge_base = {
            "HR и управление персоналом": [
                "В 2024 году компании активно внедряют AI в процессы рекрутинга - автоматизированное сканирование резюме и первичные собеседования с ботами",
                "Тренд 2024: развитие soft skills становится приоритетом - 78% компаний инвестируют в обучение эмоциональному интеллекту",
                "Гибридные форматы работы требуют новых подходов к управлению - ежедневные стендапы и цифровые доски задач",
                "Diversity & Inclusion: 65% компаний внедрили программы разнообразия в 2024 году",
                "Цифровая трансформация HR: внедрение HRIS систем и мобильных приложений для сотрудников"
            ],
            "PR и коммуникации": [
                "Видеоконтент доминирует в 2024: short-form видео увеличивает вовлеченность на 300%",
                "LinkedIn становится ключевой B2B платформой - 85% B2B компаний используют его для PR",
                "AI-генерация контента: 45% PR-специалистов используют ChatGPT для пресс-релизов",
                "Доверие к микро-инфлюенсерам растет - их рекомендации считают более authentic",
                "Data-driven PR: использование аналитики для измерения эффективности кампаний"
            ],
            "ремонт и строительство": [
                "Эко-тренды 2024: натуральные материалы и энергоэффективные решения в приоритете",
                "Умный дом становится стандартом - 60% новостроек включают системы автоматизации",
                "Модульные и сборные конструкции сокращают сроки строительства на 40%",
                "Биофильный дизайн: интеграция природы в интерьеры офисов и жилых пространств",
                "Цифровизация стройки: использование BIM-моделирования и дронов для контроля"
            ]
        }
        
    def load_post_history(self):
        try:
            if os.path.exists(self.history_file):
                with open(self.history_file, 'r', encoding='utf-8') as f:
                    return json.load(f)
            return {}
        except:
            return {}
    
    def save_post_history(self):
        try:
            with open(self.history_file, 'w', encoding='utf-8') as f:
                json.dump(self.post_history, f, ensure_ascii=False, indent=2)
        except Exception as e:
            print(f"⚠️ Ошибка сохранения истории: {e}")
    
    def generate_post_hash(self, text):
        return hashlib.md5(text.encode('utf-8')).hexdigest()
    
    def is_post_unique(self, post_text, channel_id):
        post_hash = self.generate_post_hash(post_text)
        channel_key = str(channel_id)
        
        if channel_key not in self.post_history:
            self.post_history[channel_key] = []
        
        recent_posts = self.post_history[channel_key][-50:]
        return post_hash not in recent_posts
    
    def add_to_history(self, post_text, channel_id):
        post_hash = self.generate_post_hash(post_text)
        channel_key = str(channel_id)
        
        if channel_key not in self.post_history:
            self.post_history[channel_key] = []
        
        self.post_history[channel_key].append(post_hash)
        if len(self.post_history[channel_key]) > 100:
            self.post_history[channel_key] = self.post_history[channel_key][-50:]
        
        self.save_post_history()
    
    def generate_tg_post(self, theme):
        facts = random.sample(self.knowledge_base[theme], 2)
        
        prompt = f"""
        {self.author}

        СОЗДАЙ АКТУАЛЬНЫЙ ПОСТ ДЛЯ TELEGRAM НА ТЕМУ: {theme}
        ГОД: 2024-2025

        ИСПОЛЬЗУЙ ЭТИ АКТУАЛЬНЫЕ ДАННЫЕ:
        - {facts[0]}
        - {facts[1]}

        🔵 ШАБЛОН ДЛЯ TELEGRAM:

        1. Зацепка (основанная на актуальных данных)
        {{интрига или боль на основе реальных трендов}}

        ⸻

        2. Контекст (используй свежие данные)
        {{что происходит в индустрии сейчас}}

        ⸻

        3. Главная мысль
        {{вывод из актуальной информации}}

        ⸻

        4. Практическая польза
        • {{конкретный совет 1}}
        • {{конкретный совет 2}} 
        • {{конкретный совет 3}}

        ⸻

        5. Прогноз или рекомендация
        {{что делать дальше}}

        ⸻

        6. Вопрос для обсуждения
        {{основанный на реальных данных}}

        ⸻

        ТРЕБОВАНИЯ:
        ✅ Используй предоставленные актуальные данные
        ✅ Только информация за 2024-2025 годы
        ✅ Конкретные примеры и практическая польза
        ❌ Без удаленной работы и гибридных форматов
        ✅ Только офисные/реальные кейсы
        ✅ Естественный язык, без шаблонности
        """

        try:
            print(f"🧠 Генерация ТГ-поста: {theme}...")
            url = f"https://generativelanguage.googleapis.com/v1/models/gemini-1.5-flash:generateContent?key={GEMINI_API_KEY}"
            
            response = requests.post(
                url,
                json={
                    "contents": [{
                        "parts": [{"text": prompt}]
                    }],
                    "generationConfig": {
                        "maxOutputTokens": 800,
                        "temperature": 0.9,
                    }
                },
                timeout=30
            )
            
            if response.status_code == 200:
                data = response.json()
                post_text = data["candidates"][0]["content"]["parts"][0]["text"].strip()
                
                banned_keywords = ["удален", "гибридн", "remote", "home office", "дистанц"]
                for keyword in banned_keywords:
                    if keyword in post_text.lower():
                        print(f"🚫 ОБНАРУЖЕН ЗАПРЕЩЕННЫЙ КЛЮЧ: {keyword}")
                        return self.generate_fallback_post(theme, is_tg=True)
                
                print(f"✅ ТГ-пост готов ({len(post_text)} символов)")
                return post_text
            else:
                print(f"❌ Ошибка Gemini: {response.status_code}")
                return self.generate_fallback_post(theme, is_tg=True)
                
        except Exception as e:
            print(f"❌ Ошибка генерации ТГ: {e}")
            return self.generate_fallback_post(theme, is_tg=True)
    
    def generate_zen_post(self, theme):
        facts = random.sample(self.knowledge_base[theme], 3)
        
        prompt = f"""
        {self.author}

        Тренды Дзена 2025: структурность, читабельность, аналитика, подзаголовки.

        СОЗДАЙ АНАЛИТИЧЕСКИЙ ПОСТ ДЛЯ ЯНДЕКС.ДЗЕН НА ТЕМУ: {theme}
        ГОД: 2024-2025

        ИСПОЛЬЗУЙ ЭТИ АКТУАЛЬНЫЕ ДАННЫЕ:
        - {facts[0]}
        - {facts[1]}
        - {facts[2]}

        🟡 ШАБЛОН ДЛЯ ЯНДЕКС.ДЗЕНА:

        1. Заголовок (SEO + интрига на основе актуальных данных)

        ⸻

        2. Интро (вводный абзац о текущей ситуации в индустрии)

        ⸻

        3. Анализ проблемы (используй реальные данные)

        ⸻

        4. Ключевой инсайт (основанный на свежих трендах)

        ⸻

        5. Структурный анализ с подзаголовками:

        • Актуальные тренды 2024-2025
        {{на основе предоставленных данных}}

        • Статистика и реальные данные  
        {{конкретные цифры и примеры}}

        • Практические рекомендации
        {{что делать в текущей ситуации}}

        ⸻

        6. Прогноз развития (на основе текущих данных)

        ⸻

        7. Выводы и рекомендации

        ⸻

        ТРЕБОВАНИЯ:
        ✅ Используй предоставленные актуальные данные
        ✅ Аналитика на основе реальной информации 2024-2025
        ✅ Структура с подзаголовками
        ✅ Конкретные примеры и статистика
        ❌ Без эмодзи, хештегов, упоминаний
        ❌ Без удаленной работы
        ✅ Только офисные/реальные бизнес-кейсы
        """

        try:
            print(f"🧠 Генерация Дзен-поста: {theme}...")
            url = f"https://generativelanguage.googleapis.com/v1/models/gemini-1.5-flash:generateContent?key={GEMINI_API_KEY}"
            
            response = requests.post(
                url,
                json={
                    "contents": [{
                        "parts": [{"text": prompt}]
                    }],
                    "generationConfig": {
                        "maxOutputTokens": 1000,
                        "temperature": 0.8,
                    }
                },
                timeout=30
            )
            
            if response.status_code == 200:
                data = response.json()
                post_text = data["candidates"][0]["content"]["parts"][0]["text"].strip()
                
                banned_keywords = ["удален", "гибридн", "remote", "home office", "дистанц"]
                for keyword in banned_keywords:
                    if keyword in post_text.lower():
                        print(f"🚫 ОБНАРУЖЕН ЗАПРЕЩЕННЫЙ КЛЮЧ: {keyword}")
                        return self.generate_fallback_post(theme, is_tg=False)
                
                print(f"✅ Дзен-пост готов ({len(post_text)} символов)")
                return post_text
            else:
                print(f"❌ Ошибка Gemini: {response.status_code}")
                return self.generate_fallback_post(theme, is_tg=False)
                
        except Exception as e:
            print(f"❌ Ошибка генерации Дзен: {e}")
            return self.generate_fallback_post(theme, is_tg=False)
    
    def generate_fallback_post(self, theme, is_tg=True):
        facts = random.sample(self.knowledge_base[theme], 2)
        
        if is_tg:
            return f"""🚀 {theme.upper()} 2024-2025: АКТУАЛЬНЫЕ ТРЕНДЫ

{facts[0]}

⸻

Современные вызовы требуют новых решений. {facts[1]}

⸻

Адаптивность становится ключевым навыком в текущих условиях.

⸻

• Внедрение цифровых инструментов
• Фокус на эффективности процессов  
• Развитие профессиональных компетенций
• Оптимизация операционных затрат

⸻

Современные решения позволяют достигать результатов быстрее и качественнее.

⸻

Какие изменения в вашей сфере наиболее значимы?

#{theme.replace(' ', '')} #тренды2025 #бизнес"""
        else:
            return f"""Актуальные тенденции {theme.lower()} 2024-2025

Анализ современных изменений и вызовов в профессиональной сфере.

⸻

{facts[0]}

⸻

Современная бизнес-среда характеризуется быстрыми изменениями и повышенными требованиями к эффективности. {facts[1]}

⸻

Основной вызов текущего периода - необходимость быстрой адаптации к меняющимся условиям при сохранении операционной эффективности.

⸻

Цифровая трансформация

Внедрение современных технологических решений становится обязательным условием конкурентоспособности в 2024-2025 годах.

Оптимизация процессов

Пересмотр традиционных подходов к организации работы позволяет высвободить ресурсы для стратегических задач.

Развитие компетенций

Непрерывное обучение и профессиональный рост персонала - ключевой фактор успеха в современных условиях.

⸻

Компании, успешно внедряющие системные изменения, демонстрируют устойчивый рост даже в условиях нестабильности.

⸻

Стратегический подход к управлению изменениями позволяет не только адаптироваться к текущим вызовам, но и создавать основу для будущего развития."""

    def add_tg_hashtags(self, theme):
        hashtags = {
            "HR и управление персоналом": "#HR #управление #команда #офис #персонал #2025",
            "PR и коммуникации": "#PR #коммуникации #бренд #медиа #маркетинг #2025", 
            "ремонт и строительство": "#ремонт #стройка #дизайн #интерьер #квартира #2025"
        }
        return hashtags.get(theme, "#2025")
    
    def get_theme_image(self, theme):
        return random.choice(self.theme_images.get(theme, self.theme_images["HR и управление персоналом"]))
    
    def send_to_telegram(self, chat_id, text, image_url):
        url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendPhoto"
        payload = {
            "chat_id": chat_id,
            "photo": image_url,
            "caption": text,
            "parse_mode": "HTML"
        }
        
        try:
            response = requests.post(url, json=payload, timeout=30)
            if response.status_code == 200:
                self.add_to_history(text, chat_id)
                print(f"✅ Пост отправлен в {chat_id}")
                return True
            else:
                print(f"❌ Ошибка отправки в {chat_id}: {response.text}")
                return False
        except Exception as e:
            print(f"❌ Ошибка отправки: {e}")
            return False
    
    def send_dual_posts(self):
        theme = random.choice(self.themes)
        print(f"🎯 Выбрана тема: {theme}")
        
        theme_image = self.get_theme_image(theme)
        print(f"🖼️ Фото для темы: {theme_image}")
        
        print("🧠 Генерация постов...")
        tg_post = self.generate_tg_post(theme)
        zen_post = self.generate_zen_post(theme)
        
        tg_full_post = f"{tg_post}\n\n{self.add_tg_hashtags(theme)}"
        
        print(f"📝 ТГ-пост: {len(tg_full_post)} символов")
        print(f"📝 Дзен-пост: {len(zen_post)} символов")
        
        if not self.is_post_unique(tg_full_post, MAIN_CHANNEL_ID):
            print("⚠️ Пост для ТГ не уникален, генерируем заново...")
            return self.send_dual_posts()
            
        if not self.is_post_unique(zen_post, ZEN_CHANNEL_ID):
            print("⚠️ Пост для Дзена не уникален, генерируем заново...")  
            return self.send_dual_posts()
        
        print("📤 Отправка в основной канал @da4a_hr...")
        tg_success = self.send_to_telegram(MAIN_CHANNEL_ID, tg_full_post, theme_image)
        
        print("📤 Отправка в Дзен-канал @tehdzenm...")
        zen_success = self.send_to_telegram(ZEN_CHANNEL_ID, zen_post, theme_image)
        
        if tg_success and zen_success:
            print("✅ ПОСТЫ УСПЕШНО ОТПРАВЛЕНЫ!")
            return True
        else:
            print(f"💥 ОШИБКИ: ТГ={tg_success}, Дзен={zen_success}")
            return False

def main():
    print("\n🚀 ЗАПУСК УМНОГО ГЕНЕРАТОРА")
    print("🎯 Актуальная информация 2024-2025")
    print("🎯 База знаний с реальными данными")
    print("🎯 Бесплатный Gemini API")
    print("=" * 80)
    
    bot = SmartPostGenerator()
    success = bot.send_dual_posts()
    
    if success:
        print("\n🎉 УСПЕХ! Посты отправлены в оба канала!")
    else:
        print("\n💥 ЕСТЬ ОШИБКИ ОТПРАВКИ!")
    
    print("=" * 80)

if __name__ == "__main__":
    main()
