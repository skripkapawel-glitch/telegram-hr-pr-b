import os
import requests
import random
import json
import hashlib
from dotenv import load_dotenv

load_dotenv()

BOT_TOKEN = os.getenv("BOT_TOKEN")
MAIN_CHANNEL_ID = "@da4a_hr"
ZEN_CHANNEL_ID = "@tehdzenm"
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")

print("=" * 80)
print("🚀 УМНЫЙ БОТ: ПРОФЕССИОНАЛЬНЫЕ ПОСТЫ")
print("=" * 80)

class SmartPostGenerator:
    def __init__(self):
        self.themes = ["HR и управление персоналом", "PR и коммуникации", "ремонт и строительство"]
        
        self.history_file = "post_history.json"
        self.post_history = self.load_post_history()
        
        # Надежные изображения
        self.theme_images = {
            "HR и управление персоналом": [
                "https://images.unsplash.com/photo-1552664730-d307ca884978?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
                "https://images.unsplash.com/photo-1542744173-8e7e53415bb0?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
            ],
            "PR и коммуникации": [
                "https://images.unsplash.com/photo-1432888622747-4eb9a8efeb07?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
                "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?ixlib=rb-4.0.3&w=1200&h=630&fit=crop", 
            ],
            "ремонт и строительство": [
                "https://images.unsplash.com/photo-1541888946425-d81bb19240f5?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
                "https://images.unsplash.com/photo-1504307651254-35680f356dfd?ixlib=rb-4.0.3&w=1200&h=630&fit=crop",
            ]
        }
        
        self.fallback_images = [
            "https://picsum.photos/1200/630",
            "https://placekitten.com/1200/630",
        ]
        
        # База знаний с качественным контентом
        self.knowledge_base = {
            "HR и управление персоналом": [
                {
                    "title": "HR в 2024: нанимают не навыки, а mindset",
                    "facts": [
                        "78% компаний ищут кандидатов с развитым эмоциональным интеллектом",
                        "AI уже обрабатывает 65% резюме на первичном этапе"
                    ],
                    "tg_tips": [
                        "Смотрите на soft skills — техническим навыкам можно научить, а мышление меняется долго",
                        "Внедрите пробный день вместо пятиэтапного собеседования — так понятнее и кандидату, и вам",
                        "Давайте обратную связь даже тем, кого не взяли — это формирует лояльность к бренду работодателя"
                    ],
                    "zen_structure": [
                        "Мягкие навыки вместо жестких требований",
                        "Прозрачность процесса найма", 
                        "Постоянная обратная связь"
                    ]
                },
                {
                    "title": "Как удерживать команду в 2024",
                    "facts": [
                        "Сотрудники остаются там, где есть развитие — 68% ценят обучение выше зарплаты",
                        "Гибкий график стал стандартом ожидания, а не преимуществом"
                    ],
                    "tg_tips": [
                        "Создайте карту развития для каждой позиции — люди должны видеть свой рост",
                        "Разрешите работать над личными проектами 10% времени — это снижает выгорание",
                        "Проводите регулярные one-to-one встречи не о задачах, а о целях и настроении"
                    ],
                    "zen_structure": [
                        "Карьерный рост вместо денежных бонусов",
                        "Баланс работы и личных интересов",
                        "Открытый диалог с руководством"
                    ]
                }
            ],
            "PR и коммуникации": [
                {
                    "title": "PR в 2024: что реально работает",
                    "facts": [
                        "LinkedIn стал главной B2B-площадкой — 85% компаний ведут там PR",
                        "Короткие видео дают +300% к вовлеченности compared с текстом"
                    ],
                    "tg_tips": [
                        "Делайте личный бренд руководителей в LinkedIn — люди доверяют людям, а не компаниям",
                        "Снимите 30-секундное видео о проекте вместо пресс-релиза — его досмотрят до конца", 
                        "Дайте эксперту из команды пообщаться в комментариях — это живая коммуникация, а не монолог"
                    ],
                    "zen_structure": [
                        "Личный бренд вместо корпоративного",
                        "Видеоконтент вместо текстового",
                        "Диалог с аудиторией вместо монолога"
                    ]
                },
                {
                    "title": "Коммуникации, которые действительно доходят до людей",
                    "facts": [
                        "45% PR-специалистов используют AI для подготовки материалов",
                        "Аудитория в 3 раза чаще доверяет микро-инфлюенсерам, чем знаменитостям"
                    ],
                    "tg_tips": [
                        "Говорите на языке клиента, а не на корпоративном жаргоне — проверьте, поймет ли ваша бабушка",
                        "Показывайте закулисье проектов — прозрачность builds trust быстрее, чем идеальная картинка",
                        "Отвечайте на комментарии лично — это превращает пассивную аудиторию в сообщество"
                    ],
                    "zen_structure": [
                        "Простой язык вместо профессионального жаргона",
                        "Прозрачность процессов и решений", 
                        "Сообщество вместо аудитории"
                    ]
                }
            ],
            "ремонт и строительство": [
                {
                    "title": "Ремонт в 2024: тренды, которые останутся",
                    "facts": [
                        "Натуральные материалы выбирают 72% клиентов — даже если дороже",
                        "Умный дом стал must-have для 60% новостроек"
                    ],
                    "tg_tips": [
                        "Используйте локальные материалы — это экологичнее и поддерживает местных производителей", 
                        "Заложите умные системы на этапе черновой отделки — переделывать будет дороже",
                        "Сделайте универсальную базу — сменный декор обойдется дешевле, чем новый ремонт"
                    ],
                    "zen_structure": [
                        "Натуральные и локальные материалы",
                        "Технологии как основа, а не дополнение",
                        "Гибкость и адаптивность пространства"
                    ]
                },
                {
                    "title": "Строительство без стресса: как сейчас работают",
                    "facts": [
                        "Модульные конструкции сокращают сроки на 40% без потери качества",
                        "Дроны и 3D-сканирование экономят до 25% бюджета на измерениях"
                    ],
                    "tg_tips": [
                        "Создайте цифровой двойник объекта — это поможет избежать 80% ошибок на стройке",
                        "Используйте BIM-моделирование — видите clashes до начала работ, а не в процессе",
                        "Ведите онлайн-дневник стройки — клиент в курсе прогресса, вы меньше отвлекаетесь на отчеты"
                    ],
                    "zen_structure": [
                        "Цифровое проектирование и контроль",
                        "Прозрачность для заказчика",
                        "Оптимизация процессов через технологии"
                    ]
                }
            ]
        }

    def load_post_history(self):
        try:
            if os.path.exists(self.history_file):
                with open(self.history_file, 'r', encoding='utf-8') as f:
                    return json.load(f)
            return {}
        except:
            return {}
    
    def save_post_history(self):
        try:
            with open(self.history_file, 'w', encoding='utf-8') as f:
                json.dump(self.post_history, f, ensure_ascii=False, indent=2)
        except Exception as e:
            print(f"⚠️ Ошибка сохранения истории: {e}")
    
    def get_reliable_image(self, theme):
        try:
            theme_image = random.choice(self.theme_images.get(theme, self.theme_images["HR и управление персоналом"]))
            print(f"🖼️ Используем изображение: {theme_image}")
            return theme_image
        except:
            fallback = random.choice(self.fallback_images)
            print(f"🖼️ Используем fallback: {fallback}")
            return fallback
    
    def generate_tg_post(self, theme_data):
        """Генерирует пост для Telegram в человеческом стиле"""
        theme = theme_data["theme"]
        content = theme_data["content"]
        
        # Собираем пост для ТГ
        post_parts = []
        
        # Заголовок
        post_parts.append(f"{content['title']}\n")
        
        # Факты в естественной форме
        facts_text = " • ".join(content['facts'])
        post_parts.append(f"{facts_text}\n")
        
        # Советы
        post_parts.append("Что работает прямо сейчас:")
        for i, tip in enumerate(content['tg_tips'], 1):
            post_parts.append(f"\n{tip}")
        
        # Вопрос для вовлечения
        post_parts.append(f"\n\nА в вашей практике что сработало?")
        
        return "\n".join(post_parts)
    
    def generate_zen_post(self, theme_data):
        """Генерирует пост для Дзена в аналитическом стиле"""
        theme = theme_data["theme"]
        content = theme_data["content"]
        
        # Собираем пост для Дзена
        post_parts = []
        
        # Заголовок
        post_parts.append(f"{content['title']}\n")
        
        # Введение с фактами
        intro = f"{content['facts'][0]} {content['facts'][1]}"
        post_parts.append(f"{intro}\n")
        
        # Структурный анализ
        post_parts.append("Ключевые направления:\n")
        
        for i, point in enumerate(content['zen_structure'], 1):
            post_parts.append(f"{point}")
            # Добавляем пояснение к каждому пункту
            if i == 1:
                post_parts.append(f"{content['tg_tips'][0]}\n")
            elif i == 2:
                post_parts.append(f"{content['tg_tips'][1]}\n")
            else:
                post_parts.append(f"{content['tg_tips'][2]}\n")
        
        # Заключение
        post_parts.append("Профессионалы теперь должны совмещать экспертизу в своей области с пониманием современных инструментов и запросов аудитории. Успех приходит к тем, кто говорит с людьми на их языке.")
        
        return "\n".join(post_parts)
    
    def add_tg_hashtags(self, theme):
        hashtags = {
            "HR и управление персоналом": "#HR #управление #команда #карьера #работа",
            "PR и коммуникации": "#PR #коммуникации #LinkedIn #маркетинг #бренд", 
            "ремонт и строительство": "#ремонт #стройка #дизайн #интерьер #квартира"
        }
        return hashtags.get(theme, "")
    
    def send_to_telegram(self, chat_id, text, image_url=None):
        """Отправляет пост в Telegram"""
        print(f"📤 Отправка в {chat_id}...")
        
        if image_url:
            url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendPhoto"
            payload = {
                "chat_id": chat_id,
                "photo": image_url,
                "caption": text,
                "parse_mode": "HTML"
            }
            
            try:
                response = requests.post(url, json=payload, timeout=30)
                if response.status_code == 200:
                    self.add_to_history(text, chat_id)
                    print(f"✅ Пост отправлен в {chat_id}")
                    return True
                else:
                    print(f"❌ Ошибка: {response.text}")
                    return self.send_text_to_telegram(chat_id, text)
            except Exception as e:
                print(f"❌ Ошибка: {e}")
                return self.send_text_to_telegram(chat_id, text)
        else:
            return self.send_text_to_telegram(chat_id, text)
    
    def send_text_to_telegram(self, chat_id, text):
        """Отправляет текстовый пост"""
        url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
        payload = {
            "chat_id": chat_id,
            "text": text,
            "parse_mode": "HTML"
        }
        
        try:
            response = requests.post(url, json=payload, timeout=30)
            if response.status_code == 200:
                self.add_to_history(text, chat_id)
                print(f"✅ Текстовый пост отправлен в {chat_id}")
                return True
            else:
                print(f"❌ Ошибка: {response.text}")
                return False
        except Exception as e:
            print(f"❌ Ошибка: {e}")
            return False
    
    def generate_post_hash(self, text):
        return hashlib.md5(text.encode('utf-8')).hexdigest()
    
    def is_post_unique(self, post_text, channel_id):
        post_hash = self.generate_post_hash(post_text)
        channel_key = str(channel_id)
        
        if channel_key not in self.post_history:
            self.post_history[channel_key] = []
        
        recent_posts = self.post_history[channel_key][-50:]
        return post_hash not in recent_posts
    
    def add_to_history(self, post_text, channel_id):
        post_hash = self.generate_post_hash(post_text)
        channel_key = str(channel_id)
        
        if channel_key not in self.post_history:
            self.post_history[channel_key] = []
        
        self.post_history[channel_key].append(post_hash)
        if len(self.post_history[channel_key]) > 100:
            self.post_history[channel_key] = self.post_history[channel_key][-50:]
        
        self.save_post_history()
    
    def send_dual_posts(self):
        # Выбираем тему и конкретный контент
        theme = random.choice(self.themes)
        content = random.choice(self.knowledge_base[theme])
        
        theme_data = {
            "theme": theme,
            "content": content
        }
        
        print(f"🎯 Тема: {theme}")
        print(f"📄 Контент: {content['title']}")
        
        # Получаем изображение
        theme_image = self.get_reliable_image(theme)
        
        # Генерируем посты
        print("🧠 Генерация постов...")
        tg_post = self.generate_tg_post(theme_data)
        zen_post = self.generate_zen_post(theme_data)
        
        # Добавляем хештеги только для ТГ
        tg_full_post = f"{tg_post}\n\n{self.add_tg_hashtags(theme)}"
        
        print(f"📝 ТГ-пост: {len(tg_full_post)} символов")
        print(f"📝 Дзен-пост: {len(zen_post)} символов")
        
        # Проверяем уникальность
        if not self.is_post_unique(tg_full_post, MAIN_CHANNEL_ID):
            print("⚠️ Пост для ТГ не уникален, генерируем заново...")
            return self.send_dual_posts()
            
        if not self.is_post_unique(zen_post, ZEN_CHANNEL_ID):
            print("⚠️ Пост для Дзена не уникален, генерируем заново...")  
            return self.send_dual_posts()
        
        # Отправляем
        print("📤 Отправка в @da4a_hr...")
        tg_success = self.send_to_telegram(MAIN_CHANNEL_ID, tg_full_post, theme_image)
        
        print("📤 Отправка в @tehdzenm...")
        zen_success = self.send_to_telegram(ZEN_CHANNEL_ID, zen_post, theme_image)
        
        if tg_success and zen_success:
            print("✅ ПОСТЫ УСПЕШНО ОТПРАВЛЕНЫ!")
            return True
        else:
            print(f"⚠️ Есть ошибки: ТГ={tg_success}, Дзен={zen_success}")
            return tg_success or zen_success

def main():
    print("\n🚀 ЗАПУСК ПРОФЕССИОНАЛЬНОГО ГЕНЕРАТОРА")
    print("🎯 Человеческий язык вместо корпоративного")
    print("🎯 Практические советы вместо теории")
    print("=" * 80)
    
    bot = SmartPostGenerator()
    success = bot.send_dual_posts()
    
    if success:
        print("\n🎉 УСПЕХ! Профессиональные посты отправлены!")
    else:
        print("\n💥 ЕСТЬ ОШИБКИ ОТПРАВКИ!")
    
    print("=" * 80)

if __name__ == "__main__":
    main()
